import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    card: {
        backgroundColor: '#333333',
        padding: 16,
        borderRadius: 10,
        borderColor: '#262626',
        borderWidth: 1
    },
    text: {
        color: '#fff',
    }
})